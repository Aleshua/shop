DATABASE_USER="user"
DATABASE_PASSWORD="password"
DATABASE_HOST="database"
DATABASE_PORT="5432"

DATABASE_PATH="postgres://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?sslmode=disable"
